name: React webapp Unit Tests

on:
  push:
  pull_request:

jobs:
  test:
    name: Run Vitest
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./Autodeal-web-app

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: ./Autodeal-web-app/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npm run test:run